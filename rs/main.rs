use std::collections::HashMap;
use std::ffi::CString;
use std::os::raw::c_char;

#[link(name = "curl")]
extern {
}

fn display_uname() {
}

fn fetch_file() {
}

fn main() {
  display_uname();
  fetch_file();
}
